{
  "name": "example-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --hot src/index.ts",
    "preview": "wrangler dev",
    "build": "bunx --bun buncf build",
    "deploy": "bun run build --production && wrangler deploy",
    "test": "bun test"
  },
  "dependencies": {
    "buncf": "workspace:*",
    "wrangler": "latest"
  }
}
