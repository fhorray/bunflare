[serve.static]
plugins = ["bun-plugin-tailwind"]
env = "BUN_PUBLIC_*"

# Scripts to run before executing a file or script
# This registers the cloudflare plugin to shim bun:sqlite etc. automatically
preload = ["buncf/preload"]
